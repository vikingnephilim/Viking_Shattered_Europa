eng_british_conquest_minor_slot_1 = {
	slot = 1
	generic = no
	ai = yes
	potential_on_load = {
		has_dlc = "Rule Britannia"
	}
	potential = {
		OR = {
			tag = WSX
			tag = LOD
			tag = EAL
			tag = NOL
			tag = MRC
			tag = ENG
			tag = GBR
		}
		NOT = {
			map_setup = map_setup_random
		}
	}
	has_country_shield = yes
	VN_english_minor_build_capital = {
		icon = mission_assemble_an_army
		required_missions = {
		}
		position = 1
		trigger = {
			is_subject = no
			capital_scope = {
				fort_level = 1
			}
			capital_scope = {
				development = 6
			}
		}
		effect = {
			scottish_marches_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			yorkshire_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			west_midlands_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			east_midlands_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			wessex_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			home_counties_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			east_anglia_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			wales_area = {
				limit = {
					NOT = {
						is_core = ROOT
					}
					NOT = {
						is_permanent_claim = ROOT
					}
				}
			}
			add_historical_rival = LOD
			add_historical_rival = EAL
			add_historical_rival = NOL
			add_historical_rival = MRC
			add_historical_rival = CRN
			add_historical_rival = WLS
			add_historical_rival = SCO
			add_historical_rival = HSC
		}
	}
}

eng_british_conquest_minor_slot_2 = {
	slot = 2
	generic = no
	ai = yes
	potential_on_load = {
		has_dlc = "Rule Britannia"
	}
	potential = {
		OR = {
			tag = WSX
			tag = LOD
			tag = EAL
			tag = NOL
			tag = MRC
			tag = ENG
			tag = GBR
		}
		NOT = {
			map_setup = map_setup_random
		}
	}
	has_country_shield = yes
	VN_conquer_scottish_and_welsh_borders = {
		icon = mission_assemble_an_army
		required_missions = {
		}
		position = 1
		trigger = {
			scottish_marches_area = {
				type = all
				country_or_subject_holds = ROOT
			}
			wales_area = {
				type = all
				country_or_subject_holds = ROOT
			}
		}
		effect = {
			add_prestige = 20
		}
	}
}

eng_british_conquest_minor_slot_3 = {
	slot = 3
	generic = no
	ai = yes
	potential_on_load = {
		has_dlc = "Rule Britannia"
	}
	potential = {
		OR = {
			tag = WSX
			tag = LOD
			tag = EAL
			tag = NOL
			tag = MRC
			tag = ENG
			tag = GBR
		}
		NOT = {
			map_setup = map_setup_random
		}
	}
	has_country_shield = yes
	VN_conquer_midlands_yorkshire = {
		icon = mission_assemble_an_army
		required_missions = {
		}
		position = 1
		trigger = {
			yorkshire_area = {
				type = all
				country_or_subject_holds = ROOT
			}
			east_midlands_area = {
				type = all
				country_or_subject_holds = ROOT
			}
		}
		effect = {
			add_prestige = 20
		}
	}
}

eng_british_conquest_minor_slot_4 = {
	slot = 4
	generic = no
	ai = yes
	potential_on_load = {
		has_dlc = "Rule Britannia"
	}
	potential = {
		OR = {
			tag = WSX
			tag = LOD
			tag = EAL
			tag = NOL
			tag = MRC
			tag = ENG
			tag = GBR
		}
		NOT = {
			map_setup = map_setup_random
		}
	}
	has_country_shield = yes
	VN_conquer_wessex_west_midlands = {
		icon = mission_assemble_an_army
		required_missions = {
		}
		position = 1
		trigger = {
			wessex_area = {
				type = all
				country_or_subject_holds = ROOT
			}
			west_midlands_area = {
				type = all
				country_or_subject_holds = ROOT
			}
		}
		effect = {
			add_prestige = 20
		}
	}
}

eng_british_conquest_minor_slot_5 = {
	slot = 5
	generic = no
	ai = yes
	potential_on_load = {
		has_dlc = "Rule Britannia"
	}
	potential = {
		OR = {
			tag = WSX
			tag = LOD
			tag = EAL
			tag = NOL
			tag = MRC
			tag = ENG
			tag = GBR
		}
		NOT = {
			map_setup = map_setup_random
		}
	}
	has_country_shield = yes
	VN_conquer_anglia_and_london = {
		icon = mission_assemble_an_army
		required_missions = {
		}
		position = 1
		trigger = {
			home_counties_area = {
				type = all
				country_or_subject_holds = ROOT
			}
			east_anglia_area = {
				type = all
				country_or_subject_holds = ROOT
			}
		}
		effect = {
			add_prestige = 20
		}
	}
}
